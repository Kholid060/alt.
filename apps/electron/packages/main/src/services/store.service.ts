class StoreService {
  static installExtension(extensionId: string) {
    console.log('install', extensionId);
  }

  static installWorkflow(workflowId: string) {

  }
}

export default StoreService;
